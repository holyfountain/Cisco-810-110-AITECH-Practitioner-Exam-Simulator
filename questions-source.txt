## Question 1 (Domain: Generative AI Models)
A financial institution wants to leverage an LLM to extract sensitive customer data from internal documents. The data privacy policy strictly prohibits sending any text outside the corporate network. Which deployment strategy meets this requirement while maintaining control over data sovereignty?

* A. Multi-tenant SaaS API endpoint with TLS 1.3 encryption
* B. On-premises deployment of an open-source model using local hardware
* C. Hybrid cloud deployment utilizing public serverless endpoints
* D. Edge-compute deployment using a proprietary closed-source cloud API

Answer: 🟩 B. On-premises deployment of an open-source model using local hardware

* Why it is correct: B. On-premises deployment of an open-source model using local hardware is correct because it matches the model, retrieval, hosting, or infrastructure concept required in a scenario where a financial institution wants to leverage an LLM to extract sensitive customer data from internal documents. The data privacy policy strictly prohibits sending any text outside the corporate network.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 2 (Domain: Prompt Engineering)
A developer notices that an LLM-powered internal network assistant generates highly erratic, creative, and unpredictable troubleshooting scripts when given the same input prompt multiple times. Which parameter should the developer modify to make the model's outputs predictable, deterministic, and identical?

* A. Top-k probability filtering
* B. Max token length limits
* C. Temperature configuration to 0
* D. Context window scale

Answer: 🟩 C. Temperature configuration to 0

* Why it is correct: C. Temperature configuration to 0 is correct because it is the prompt design choice most likely to produce the requested behavior in a scenario where a developer notices that an LLM-powered internal network assistant generates highly erratic, creative, and unpredictable troubleshooting scripts when given the same input prompt multiple times.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 3 (Domain: Ethics and Security)
During an architectural review of a customer-facing AI chat agent, a security engineer flags a vulnerability where an external user could input a payload that tricks the model into executing system-level database queries via function calling. What is the most effective defense strategy against this?

* A. Hashing all incoming prompt parameters using SHA-256
* B. Implementing strict output parsing and validation using schema constraints before executing functions
* C. Increasing the model's pre-training dataset size
* D. Encrypting the vector database at rest

Answer: 🟩 B. Implementing strict output parsing and validation using schema constraints before executing functions

* Why it is correct: B. Implementing strict output parsing and validation using schema constraints before executing functions is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 4 (Domain: Development and Workflow Automation)
An engineering team integrates an AI agent into their CI/CD pipeline to analyze code pull requests. The agent's primary task is to identify security vulnerabilities like SQL injections and cross-site scripting (XSS) before deployment. What type of tool classification does this AI implementation fall under?

* A. Dynamic Application Security Testing (DAST)
* B. Automated AI-driven Static Application Security Testing (SAST)
* C. Container Runtime Image Scanning
* D. Infrastructure as Code (IaC) linting

Answer: 🟩 B. Automated AI-driven Static Application Security Testing (SAST)

* Why it is correct: B. Automated AI-driven Static Application Security Testing (SAST) is the best choice because it is the engineering practice that directly addresses a scenario where an engineering team integrates an AI agent into their CI/CD pipeline to analyze code pull requests. The agent's primary task is to identify security vulnerabilities like SQL injections and cross-site scripting (XSS) before deployment.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 5 (Domain: Generative AI Models)
What is the primary architectural purpose of the Attention Mechanism within a Transformer-based Large Language Model?

* A. To compress the model file size into smaller formats like INT8
* B. To dynamically calculate the contextual relationships and importance of tokens relative to all other tokens in a sequence
* C. To encrypt input texts into secure tokens before they hit public clouds
* D. To store long-term corporate memory instead of using a vector database

Answer: 🟩 B. To dynamically calculate the contextual relationships and importance of tokens relative to all other tokens in a sequence

* Why it is correct: B. To dynamically calculate the contextual relationships and importance of tokens relative to all other tokens in a sequence is the best answer because it is the core model or infrastructure concept that resolves the concept being tested in the question.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 6 (Domain: Prompt Engineering)
An administrator needs an LLM to classify network log messages into specific categories (e.g., Critical, Warning, Info). The base model struggles with accuracy when given just the raw log. To improve performance without fine-tuning, the administrator modifies the prompt to include three specific examples of logs and their correct target classifications. What prompt technique is being used?

* A. Zero-shot prompting
* B. Few-shot prompting
* C. Chain-of-Thought (CoT) prompting
* D. Meta-prompt engineering

Answer: 🟩 B. Few-shot prompting

* Why it is correct: B. Few-shot prompting fits best here because the question is really about prompt structure and control, and that choice matches a scenario where an administrator needs an LLM to classify network log messages into specific categories (e. g. , Critical, Warning, Info). The base model struggles with accuracy when given just the raw log. To improve performance without fine-tuning, the administrator modifies the prompt to include three specific examples of logs and their correct target classifications.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 7 (Domain: Ethics and Security)
A data science team is preparing a dataset to train an internal HR assistant model. They intentionally scrub attributes like gender, race, and age from the training corpus to prevent biased hiring recommendations. What core tenet of Cisco’s Responsible AI framework does this process directly support?

* A. Accountability
* B. Fairness and Non-discrimination
* C. Translatability
* D. Intellectual Property Protection

Answer: 🟩 B. Fairness and Non-discrimination

* Why it is correct: B. Fairness and Non-discrimination is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where a data science team is preparing a dataset to train an internal HR assistant model. They intentionally scrub attributes like gender, race, and age from the training corpus to prevent biased hiring recommendations.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 8 (Domain: Development and Workflow Automation)
When using an AI code copilot to generate unit tests for an enterprise Java application, why is it critical for human developers to perform a validation check on the generated outputs?

* A. AI copilot tools cannot generate syntactically correct code.
* B. The copilot may introduce plausible-looking but non-existent methods or libraries, commonly known as hallucinations.
* C. Unit tests generated by AI require a special runtime license from the compiler.
* D. The copilot automatically deletes original source files if the test fails.

Answer: 🟩 B. The copilot may introduce plausible-looking but non-existent methods or libraries, commonly known as hallucinations.

* Why it is correct: B. The copilot may introduce plausible-looking but non-existent methods or libraries, commonly known as hallucinations is correct because it is the workflow or delivery practice most likely to solve the problem the prompt describes.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 9 (Domain: Generative AI Models)
In a Retrieval-Augmented Generation (RAG) architecture, what is the core role of a Vector Database?

* A. It processes natural language instructions into floating-point math vectors.
* B. It holds indexed high-dimensional vector embeddings to enable rapid semantic similarity searches.
* C. It acts as the primary web crawler to find real-time internet data.
* D. It compiles the final Python code generated by the LLM.

Answer: 🟩 B. It holds indexed high-dimensional vector embeddings to enable rapid semantic similarity searches.

* Why it is correct: B. It holds indexed high-dimensional vector embeddings to enable rapid semantic similarity searches. fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind in a Retrieval-Augmented Generation (RAG) architecture, what is the core role of a Vector Database.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 10 (Domain: Generative AI Models)
An enterprise application issues 50,000 API calls daily to an LLM provider. Each call contains a system instruction of 500 tokens and a user prompt of 100 tokens. The developer optimizes the architecture by moving the 500-token system instruction to an API-level cache layer that reduces the billed input size to 50 tokens per call. What is the daily token reduction achieved by this optimization?

* A. 15,000,000 tokens
* B. 20,000,000 tokens
* C. 22,500,000 tokens
* D. 25,000,000 tokens

Answer: 🟩 C. 22,500,000 tokens

* Why it is correct: C. 22,500,000 tokens is correct because it matches the model, retrieval, hosting, or infrastructure concept required in a scenario where an enterprise application issues 50,000 API calls daily to an LLM provider. Each call contains a system instruction of 500 tokens and a user prompt of 100 tokens. The developer optimizes the architecture by moving the 500-token system instruction to an API-level cache layer that reduces the billed input size to 50 tokens per call.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 11 (Domain: Generative AI Models)
An attacker compromises an enterprise vector database by injecting malicious text chunks filled with false information. As a result, when employees use the RAG-enabled chatbot, it outputs completely fabricated operational guidelines. Which type of security threat does this behavior represent?

* A. Model Evasion
* B. Data Poisoning / Knowledge Corruption
* C. Prompt Leaking
* D. Denial of Service (DoS)

Answer: 🟩 B. Data Poisoning / Knowledge Corruption

* Why it is correct: B. Data Poisoning / Knowledge Corruption is the best answer because it is the core model or infrastructure concept that resolves a scenario where an attacker compromises an enterprise vector database by injecting malicious text chunks filled with false information. As a result, when employees use the RAG-enabled chatbot, it outputs completely fabricated operational guidelines.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 12 (Domain: Agentic AI)
Which open architectural framework allows LLMs to interact standardizedly with disparate external tools, databases, and environments without writing custom APIs for every tool?

* A. Model Quantization Protocol (MQP)
* B. Model Context Protocol (MCP)
* C. Retrieval-Augmented Compression (RAC)
* D. JSON Web Token (JWT)

Answer: 🟩 B. Model Context Protocol (MCP)

* Why it is correct: B. Model Context Protocol (MCP) is the strongest answer because the scenario is testing agent control flow, and that option best reflects how the agent should behave.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 13 (Domain: Development and Workflow Automation)
A developer wants an AI agent to automatically generate architectural documentation directly from a massive legacy repository containing hundreds of interconnected C++ source files. Which constraint represents the primary technological barrier to processing this entire codebase in a single LLM request?

* A. The model's context window limit
* B. The temperature scale restriction
* C. Token serialization speeds
* D. The system's hardware cooling profile

Answer: 🟩 A. The model's context window limit

* Why it is correct: A. The model's context window limit is the best choice because it is the engineering practice that directly addresses a scenario where a developer wants an AI agent to automatically generate architectural documentation directly from a massive legacy repository containing hundreds of interconnected C++ source files.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 14 (Domain: Prompt Engineering)
An engineer designs an emergency network triage agent. They want the agent to reason through a complex infrastructure failure step-by-step before outputting the final mitigation script, ensuring that logical errors can be easily audited. Which prompt technique should be utilized?

* A. Role-based persona shifting
* B. Chain-of-Thought (CoT) prompting
* C. Negative prompting structures
* D. Context window padding

Answer: 🟩 B. Chain-of-Thought (CoT) prompting

* Why it is correct: B. Chain-of-Thought (CoT) prompting is correct because it is the prompt design choice most likely to produce the requested behavior in a scenario where an engineer designs an emergency network triage agent. They want the agent to reason through a complex infrastructure failure step-by-step before outputting the final mitigation script, ensuring that logical errors can be easily audited.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 15 (Domain: Ethics and Security)
A Cisco customer implements an automated AI model to monitor network health. According to global compliance standards and AI safety frameworks, what mechanism must be in place if the model detects a critical outage and suggests a routing topology change?

* A. The model must automatically apply the changes without notification.
* B. A Human-in-the-Loop (HITL) approval step to validate the fix before deployment.
* C. The model must scramble its baseline parameters to ensure anonymity.
* D. An automated script to shut down the model permanently upon error discovery.

Answer: 🟩 B. A Human-in-the-Loop (HITL) approval step to validate the fix before deployment.

* Why it is correct: B. A Human-in-the-Loop (HITL) approval step to validate the fix before deployment. is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 16 (Domain: Data Research and Analysis)
During automated data preparation, an AI system flags missing values and inconsistent formats across multiple data sources. This step occurs before analysis or modeling begins. Which task category is the AI primarily performing?

* A. Feature engineering
* B. Data quality checking and cleaning
* C. Model evaluation
* D. Prompt optimization

Answer: 🟩 B. Data quality checking and cleaning

* Why it is correct: The deciding issue here is during automated data preparation, an AI system flags missing values and inconsistent formats across multiple data sources. This step occurs before analysis or modeling begins, and B. Data quality checking and cleaning is the data preparation, analysis, or evaluation step that best fits it.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 17 (Domain: Generative AI Models)
What is the primary role of a context window in generative AI systems?

* A. Enforcing ethical constraints
* B. Controlling model accuracy
* C. Defining maximum memory for a single interaction
* D. Limiting training data size

Answer: 🟩 C. Defining maximum memory for a single interaction

* Why it is correct: C. Defining maximum memory for a single interaction is the best answer because it is the core model or infrastructure concept that resolves the concept being tested in the question.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 18 (Domain: Ethics and Security)
Security teams often restrict training or prompting AI models with sensitive customer data. This reduces risk but may limit usefulness. What is the primary trade-off being managed?

* A. Performance versus privacy
* B. Capability breadth versus vendor lock-in
* C. Explainability versus automation
* D. Latency versus observability

Answer: 🟩 A. Performance versus privacy

* Why it is correct: A. Performance versus privacy is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 19 (Domain: Ethics and Security)
Why is explainability particularly important when AI systems support human decision-making? Decision-makers must trust and understand recommendations before acting on them.

* A. It guarantees lower error rates in every review workflow
* B. It enables informed oversight and challenge
* C. It replaces confidence thresholds with natural-language reasoning
* D. It removes the need for governance review before deployment

Answer: 🟩 B. It enables informed oversight and challenge

* Why it is correct: B. It enables informed oversight and challenge is correct because it is the safeguard or governance control that best addresses the core requirement in this question.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 20 (Domain: Prompt Engineering)
An AI practitioner is assessing threats unique to AI systems rather than traditional IT systems. They want to focus on risks introduced by generative capabilities. Which threat is AI-specific?

* A. Network congestion
* B. Hardware failure
* C. Distributed denial-of-service
* D. Prompt injection

Answer: 🟩 D. Prompt injection

* Why it is correct: D. Prompt injection is correct because it is the prompt design choice most likely to produce the requested behavior in a scenario where an AI practitioner is assessing threats unique to AI systems rather than traditional IT systems. They want to focus on risks introduced by generative capabilities.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 21 (Domain: Ethics and Security)
Misinformation generated by AI systems poses reputational and operational risks. This risk increases when outputs are shared without verification. Which mitigation strategy best addresses this concern?

* A. Human-in-the-loop validation
* B. Autonomous deployment without review
* C. Larger context windows
* D. Reduced logging

Answer: 🟩 A. Human-in-the-loop validation

* Why it is correct: A. Human-in-the-loop validation is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 22 (Domain: Ethics and Security)
Bias in AI systems can lead to unfair or harmful outcomes, particularly when models are used in decision-support roles. Organizations must address this risk proactively. Which action most directly helps mitigate bias?

* A. Increasing model size to improve statistical coverage
* B. Auditing training and input data for representation gaps
* C. Expanding the context window to expose more examples
* D. Removing audit trails so reviewers focus only on final outputs

Answer: 🟩 B. Auditing training and input data for representation gaps

* Why it is correct: B. Auditing training and input data for representation gaps is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where bias in AI systems can lead to unfair or harmful outcomes, particularly when models are used in decision-support roles. Organizations must address this risk proactively.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 23 (Domain: Generative AI Models)
When designing prompts for image and audio generation, practitioners often adjust structure differently than for text-only tasks. This is because multimodal outputs require clearer intent signaling. What is the main reason for this difference?

* A. Multimodal models ignore constraints
* B. Token limits do not apply to images or audio
* C. Few-shot prompting is unsupported
* D. Non-text outputs require precise guidance on format and attributes

Answer: 🟩 D. Non-text outputs require precise guidance on format and attributes

* Why it is correct: D. Non-text outputs require precise guidance on format and attributes is the best answer because it is the core model or infrastructure concept that resolves when designing prompts for image and audio generation, practitioners often adjust structure differently than for text-only tasks. This is because multimodal outputs require clearer intent signaling.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 24 (Domain: Prompt Engineering)
Few-shot prompting is often recommended when working with specialized or domain-specific tasks. This technique relies on providing examples to guide the model’s behavior. Why do examples improve model performance in these scenarios?

* A. They demonstrate desired patterns and structure
* B. They retrain the model dynamically
* C. They increase the model’s context window
* D. They reduce token consumption

Answer: 🟩 A. They demonstrate desired patterns and structure

* Why it is correct: A. They demonstrate desired patterns and structure fits best here because the question is really about prompt structure and control, and that choice matches a scenario where few-shot prompting is often recommended when working with specialized or domain-specific tasks. This technique relies on providing examples to guide the model’s behavior.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 25 (Domain: Generative AI Models)
Which use case is best suited for diffusion models rather than LLMs?

* A. Sentiment analysis
* B. Image synthesis from noise
* C. Chat-based question answering
* D. Code refactoring

Answer: 🟩 B. Image synthesis from noise

* Why it is correct: B. Image synthesis from noise is correct because it matches the model, retrieval, hosting, or infrastructure concept required in the concept being tested in the question.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 26 (Domain: Generative AI Models)
A data center engineer is building an on-premises compute cluster to handle simultaneous fine-tuning of multiple 70-billion parameter LLMs. The high-performance cluster utilizes multiple nodes, each equipped with 8x NVIDIA H100 GPUs. Which interconnect technology should be deployed between the physical server nodes to prevent network bandwidth bottlenecks during distributed training?

* A. PCIe Gen 5 expansion busses
* B. NVLink bridges
* C. InfiniBand or RoCE (RDMA over Converged Ethernet) switches
* D. Standard Gigabit Ethernet loops

Answer: 🟩 C. InfiniBand or RoCE (RDMA over Converged Ethernet) switches

* Why it is correct: C. InfiniBand or RoCE (RDMA over Converged Ethernet) switches is the best answer because it is the core model or infrastructure concept that resolves a scenario where a data center engineer is building an on-premises compute cluster to handle simultaneous fine-tuning of multiple 70-billion parameter LLMs. The high-performance cluster utilizes multiple nodes, each equipped with 8x NVIDIA H100 GPUs.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 27 (Domain: Agentic AI)
An architectural team is designing a multi-agent workflow for an automated IT Helpdesk. Agent A extracts information from a user's ticket, Agent B generates a terminal command script, and Agent C executes the command after human approval. If Agent B's output format fluctuates unpredictably, it breaks Agent C's parser. How should the developers programmatically enforce structural conformity?

* A. Increase the system temperature to 1.0.
* B. Wrap Agent B's generation using Structured Outputs (JSON Schema enforcement).
* C. Replace Agent B with a smaller, unquantized model.
* D. Apply a vector similarity search on the prompt context.

Answer: 🟩 B. Wrap Agent B's generation using Structured Outputs (JSON Schema enforcement).

* Why it is correct: B. Wrap Agent B's generation using Structured Outputs (JSON Schema enforcement). is the strongest answer because the scenario is testing agent control flow, and that option best reflects how the agent should behave.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 28 (Domain: Ethics and Security)
A network security team tracks an ongoing threat vector where remote malicious users attempt to uncover the highly classified base instructions and secret operating guidelines originally programmed into a corporate AI gateway. What specific exploit variation are the attackers executing?

* A. Training Data Extraction
* B. Prompt Leaking
* C. Vector Database DoS
* D. Direct Prompt Injection

Answer: 🟩 B. Prompt Leaking

* Why it is correct: B. Prompt Leaking is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where a network security team tracks an ongoing threat vector where remote malicious users attempt to uncover the highly classified base instructions and secret operating guidelines originally programmed into a corporate AI gateway.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 29 (Domain: Development and Workflow Automation)
An engineer uses an AI agent to automatically convert an old Python 2 script into Python 3 syntax. The script contains heavily nested loops and outdated external library dependencies. What technique should the engineer integrate into the prompt sequence to maximize the agent's code translation accuracy?

* A. Supply representative before-and-after migration examples plus dependency notes.
* B. Implement a self-correction feedback loop where the AI tests its own output against a compiler and iterates on error logs.
* C. Constrain the model to emit only syntax-translated lines without running checks.
* D. Force the output into markdown fenced blocks for readability.

Answer: 🟩 B. Implement a self-correction feedback loop where the AI tests its own output against a compiler and iterates on error logs.

* Why it is correct: B. Implement a self-correction feedback loop where the AI tests its own output against a compiler and iterates on error logs is correct because it is the workflow or delivery practice most likely to solve the problem the prompt describes.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 30 (Domain: Generative AI Models)
What is the primary technological benefit of utilizing KV (Key-Value) Caching in a production LLM inference server deployment?

* A. It permanently modifies the core model weights based on user preferences.
* B. It speeds up the generation of subsequent tokens by saving the past context's attention keys and values in memory.
* C. It encrypts data streams traversing between local hosts and external AI APIs.
* D. It automatically splits vector embeddings into multi-tenant sub-indices.

Answer: 🟩 B. It speeds up the generation of subsequent tokens by saving the past context's attention keys and values in memory.

* Why it is correct: B. It speeds up the generation of subsequent tokens by saving the past context's attention keys and values in memory. fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind what is the primary technological benefit of utilizing KV (Key-Value) Caching in a production LLM inference server deployment.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 31 (Domain: Agentic AI)
An enterprise network AI engine is granted access to live routing environments via tool-calling. When it receives a request to "check network latency," the model outputs a specific JSON structured block containing {"name": "ping_host", "arguments": {"ip": "10.0.0.1"}}. What component of an agentic architecture interprets this JSON and sends the packet across the wire?

* A. The Embedding Model
* B. The Tokenizer
* C. The Router Engine
* D. The Tool Execution Environment / Orchestrator

Answer: 🟩 D. The Tool Execution Environment / Orchestrator

* Why it is correct: This scenario depends on agent behavior and control flow, and D. The Tool Execution Environment / Orchestrator is the option that best matches a scenario where an enterprise network AI engine is granted access to live routing environments via tool-calling. When it receives a request to "check network latency," the model outputs a specific JSON structured block containing {"name": "ping_host", "arguments": {"ip": "10. 0. 0. 1"}}.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 32 (Domain: Ethics and Security)
An internal audit reveals that an AI model evaluating employee performances heavily penalizes workers who take parental or medical leave, despite those protection attributes being completely excluded from the dataset. The model learned this behavior implicitly through associated patterns like "consecutive gaps in active email logs." This scenario describes which AI risk?

* A. Explicit Data Poisoning
* B. Direct Prompt Hijacking
* C. Proxy Discrimination / Indirect Bias
* D. Inference Model Overclocking

Answer: 🟩 C. Proxy Discrimination / Indirect Bias

* Why it is correct: In a scenario where an internal audit reveals that an AI model evaluating employee performances heavily penalizes workers who take parental or medical leave, despite those protection attributes being completely excluded from the dataset. The model learned this behavior implicitly through associated patterns like "consecutive gaps in active email logs, C. Proxy Discrimination / Indirect Bias is the strongest fit because it adds the safeguard or oversight the situation is missing.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 33 (Domain: Prompt Engineering)
A developer wants to utilize an LLM to generate an infrastructure configuration file for Cisco DNA Center. They want the output formatted strictly as valid JSON without any markdown formatting text, chatty preambles, or conversational introductions. Which approach guarantees this format?

* A. Increasing model top-p filtering parameters to 1.0.
* B. Setting system instructions to mandate JSON and enabling a native JSON-mode inference setting.
* C. Running the prompt multiple times until the model complies.
* D. Maximizing the model context window parameter space.

Answer: 🟩 B. Setting system instructions to mandate JSON and enabling a native JSON-mode inference setting.

* Why it is correct: B. Setting system instructions to mandate JSON and enabling a native JSON-mode inference setting. fits best here because the question is really about prompt structure and control, and that choice matches a scenario where a developer wants to utilize an LLM to generate an infrastructure configuration file for Cisco DNA Center. They want the output formatted strictly as valid JSON without any markdown formatting text, chatty preambles, or conversational introductions.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 34 (Domain: Generative AI Models)
Why is a standard SQL database generally poorly suited for performing semantic, context-aware document retrievals in a production enterprise RAG pipeline?

* A. Standard SQL tables cannot attach metadata or source citations to passages.
* B. SQL engines operate via exact character or keyword matches rather than multidimensional vector distance equations.
* C. Standard SQL engines lack the authentication controls needed for enterprise RAG.
* D. Relational systems cannot store more than one embedding vector per document.

Answer: 🟩 B. SQL engines operate via exact character or keyword matches rather than multidimensional vector distance equations.

* Why it is correct: B. SQL engines operate via exact character or keyword matches rather than multidimensional vector distance equations is correct because it matches the model, retrieval, hosting, or infrastructure concept required in the concept being tested in the question.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 35 (Domain: Agentic AI)
An AI operations framework processes 2,000 logs per minute. To lower runtime operational costs, an infrastructure engineer wants to pass log messages through an ultra-lightweight classification model (1 Billion parameters) first. If the log is deemed critical, it escalates to a high-capacity model (70 Billion parameters). What design pattern does this setup leverage?

* A. Speculative Decoding
* B. Multi-Model Cascade / Model Routing
* C. Retrieval-Augmented Logic
* D. Context Ingestion Padding

Answer: 🟩 B. Multi-Model Cascade / Model Routing

* Why it is correct: B. Multi-Model Cascade / Model Routing is correct because it aligns with the planning, tool-use, memory, or orchestration behavior required in a scenario where an AI operations framework processes 2,000 logs per minute. To lower runtime operational costs, an infrastructure engineer wants to pass log messages through an ultra-lightweight classification model (1 Billion parameters) first. If the log is deemed critical, it escalates to a high-capacity model (70 Billion parameters).
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 36 (Domain: Generative AI Models)
To prevent corporate developers from accidentally leaking proprietary Cisco IOS source code to public third-party LLM providers, which technology solution should a network administrator deploy at the enterprise network perimeter?

* A. A standard Layer 4 stateful firewall block on all UDP connections
* B. An AI Gateway with Data Loss Prevention (DLP) regex filters and prompt interception capabilities
* C. A localized DHCP reservation pool policy change
* D. Symmetric hardware token generation modules

Answer: 🟩 B. An AI Gateway with Data Loss Prevention (DLP) regex filters and prompt interception capabilities

* Why it is correct: B. An AI Gateway with Data Loss Prevention (DLP) regex filters and prompt interception capabilities fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind to prevent corporate developers from accidentally leaking proprietary Cisco IOS source code to public third-party LLM providers, which technology solution should a network administrator deploy at the enterprise network perimeter.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 37 (Domain: Generative AI Models)
When deploying an open-source model like Llama-3 locally, a cloud engineer must allocate enough system memory. If an unquantized model has 8 billion parameters, and each parameter is represented as a 16-bit floating-point value (FP16 / 2 bytes), what is the absolute minimum amount of VRAM required just to load the model weights into memory?

* A. 4 GB
* B. 8 GB
* C. 16 GB
* D. 32 GB

Answer: 🟩 C. 16 GB

* Why it is correct: C. 16 GB is correct because it matches the model, retrieval, hosting, or infrastructure concept required in when deploying an open-source model like Llama-3 locally, a cloud engineer must allocate enough system memory.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 38 (Domain: Ethics and Security)
An enterprise development team is using an AI pipeline to auto-generate technical software reference documentation. The system operates by systematically reading raw application codebases, producing draft reference manuals, and publishing them directly to internal portals. Which risk factor must be audited closely during this pipeline?

* A. The model modifying the production database schema dynamically
* B. Propagation of outdated technical guidelines or hallucinations into the published developer documentation
* C. Hardware cooling loop failures inside the edge terminal switches
* D. Compiler compilation license expirations on the documentation server

Answer: 🟩 B. Propagation of outdated technical guidelines or hallucinations into the published developer documentation

* Why it is correct: In a scenario where an enterprise development team is using an AI pipeline to auto-generate technical software reference documentation. The system operates by systematically reading raw application codebases, producing draft reference manuals, and publishing them directly to internal portals, B. Propagation of outdated technical guidelines or hallucinations into the published developer documentation is the strongest fit because it adds the safeguard or oversight the situation is missing.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 39 (Domain: Development and Workflow Automation)
An autonomous AI agent encounters an unfamiliar error code while attempting to diagnose a software-defined network switch. Instead of failing immediately, the agent searches an internal knowledge base, reads a manual, adjusts its parameters, and tries a different sequence of debugging steps. Which architectural trait does this demonstrate?

* A. Deterministic hardcoded execution
* B. Static sequence orchestration
* C. Dynamic adaptability and tool usage
* D. Basic prompt tokenization

Answer: 🟩 C. Dynamic adaptability and tool usage

* Why it is correct: C. Dynamic adaptability and tool usage makes the most sense here because it matches the engineering constraint the scenario is actually testing.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 40 (Domain: Ethics and Security)
Under international AI compliance models and Cisco’s internal core risk framework, what documentation standard requires development teams to clearly declare a model's operational limitations, performance test baselines, training dataset breakdowns, and intended deployment constraints?

* A. A Model Card / AI Factsheet
* B. A post-incident root-cause analysis report
* C. A software bill of materials for the training and serving stack
* D. An operational system telemetry log dump

Answer: 🟩 A. A Model Card / AI Factsheet

* Why it is correct: A. A Model Card / AI Factsheet is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in under international AI compliance models and Cisco’s internal core risk framework, what documentation standard requires development teams to clearly declare a model's operational limitations, performance test baselines, training dataset breakdowns, and intended deployment constraints.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 41 (Domain: Development and Workflow Automation)
An engineering team is evaluating two different fine-tuned models for an automated network documentation summary tool. They want to programmatically score how well the model-generated summaries retain the factual essence of the source documentation without relying on expensive human reviewers. Which evaluation methodology should they implement?

* A. Perplexity (PPL) verification
* B. LLM-as-a-Judge using structured rubrics
* C. Loss convergence charting
* D. Cosine similarity of raw tokens

Answer: 🟩 B. LLM-as-a-Judge using structured rubrics

* Why it is correct: B. LLM-as-a-Judge using structured rubrics is correct because it is the workflow or delivery practice most likely to solve the problem the prompt describes.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 42 (Domain: Generative AI Models)
A developer is configuring a RAG pipeline that fetches data from thousands of network architecture PDFs. The retrieval engine works well for single sentences but completely loses context when paragraphs span across page boundaries. Which preprocessing step directly fixes this issue?

* A. Increasing model generation temperature
* B. Implementing recursive text splitting with a defined token overlap
* C. Compressing the PDFs into flat text strings without punctuation
* D. Switching from vector embeddings to exact keyword matches

Answer: 🟩 B. Implementing recursive text splitting with a defined token overlap

* Why it is correct: B. Implementing recursive text splitting with a defined token overlap fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind which preprocessing step directly fixes this issue.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 43 (Domain: Ethics and Security)
A cloud defense analyst detects that an attacker is sending thousands of subtly altered prompt payloads to a public-facing classification model. The inputs contain nearly invisible pixel perturbations or character swaps designed to force the model into misclassifying a malicious file as safe. What specific type of AI attack is occurring?

* A. Prompt Leaking
* B. Adversarial Example / Evasion Attack
* C. Data Poisoning
* D. Model Inversion

Answer: 🟩 B. Adversarial Example / Evasion Attack

* Why it is correct: B. Adversarial Example / Evasion Attack is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where a cloud defense analyst detects that an attacker is sending thousands of subtly altered prompt payloads to a public-facing classification model. The inputs contain nearly invisible pixel perturbations or character swaps designed to force the model into misclassifying a malicious file as safe.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 44 (Domain: Development and Workflow Automation)
An enterprise development team integrates an AI agent into their deployment lifecycle to automate Infrastructure as Code (IaC) validations. The agent reads Terraform manifests and flags overly permissive AWS IAM security groups before deployment. What is the main structural advantage of using AI over standard rigid regex linters for this task?

* A. AI guarantees policy-complete coverage with no false positives.
* B. AI understands semantic context and complex configuration dependencies that regex rules miss.
* C. AI can auto-remediate Terraform misconfigurations directly during the review step.
* D. AI makes deterministic policy packs and approval gates unnecessary.

Answer: 🟩 B. AI understands semantic context and complex configuration dependencies that regex rules miss.

* Why it is correct: B. AI understands semantic context and complex configuration dependencies that regex rules miss is correct because it is the workflow or delivery practice most likely to solve the problem the prompt describes.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 45 (Domain: Generative AI Models)
An infrastructure architect notices that a production LLM application experiences severe latency spikes when many users query the chatbot at the same exact time. Upon inspection, the bottleneck is caused by the model processing the initial prompt text repeatedly for concurrent users. Which inference-side optimization technique addresses this issue?

* A. Model Quantization (INT4)
* B. Continuous Batching / PagedAttention
* C. Low-Rank Adaptation (LoRA)
* D. Context Window Compression

Answer: 🟩 B. Continuous Batching / PagedAttention

* Why it is correct: B. Continuous Batching / PagedAttention fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind which inference-side optimization technique addresses this issue.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 46 (Domain: Agentic AI)
An autonomous network triage agent uses a loop structure known as ReAct (Reasoning and Acting). During an ongoing incident, the agent encounters a broken interface link. What sequence of operations does the agent execute inside a standard ReAct loop iteration?

* A. Encode -> Compress -> Decode
* B. Thought -> Action -> Observation
* C. Tokenize -> Cache -> Sample
* D. Embed -> Cluster -> Retrieve

Answer: 🟩 B. Thought -> Action -> Observation

* Why it is correct: This scenario depends on agent behavior and control flow, and B. Thought -> Action -> Observation is the option that best matches a scenario where an autonomous network triage agent uses a loop structure known as ReAct (Reasoning and Acting). During an ongoing incident, the agent encounters a broken interface link.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 47 (Domain: Ethics and Security)
A financial institution deploys an AI model to automate loan approvals. European regulators audit the system and demand to know the exact mathematical weights and text variables that led to a specific user's application rejection. To comply with this regulatory demand, what framework must the AI system support?

* A. High-Performance Compute (HPC) acceleration
* B. Explainable AI (XAI) / Model Interpretability
* C. Speculative Decoding
* D. Zero-shot prompting

Answer: 🟩 B. Explainable AI (XAI) / Model Interpretability

* Why it is correct: In a scenario where a financial institution deploys an AI model to automate loan approvals. European regulators audit the system and demand to know the exact mathematical weights and text variables that led to a specific user's application rejection, B. Explainable AI (XAI) / Model Interpretability is the strongest fit because it adds the safeguard or oversight the situation is missing.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 48 (Domain: Ethics and Security)
When configuring an AI coding assistant across an enterprise, which strategy best mitigates the legal liability risk of the model spitting out copy-pasted, copyrighted open-source code fragments inside a commercial product?

* A. Disabling retrieval from local repositories before code generation
* B. Enabling a code-citation or public-code matching filter in the assistant settings
* C. Limiting completions to short snippets under a fixed line threshold
* D. Setting the model's temperature to exactly 0

Answer: 🟩 B. Enabling a code-citation or public-code matching filter in the assistant settings

* Why it is correct: B. Enabling a code-citation or public-code matching filter in the assistant settings is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 49 (Domain: Prompt Engineering)
An AI practitioner is reviewing a validation report for a fine-tuned technical support model. The report lists a metric called ROUGE (Recall-Oriented Understudy for Gisting Evaluation). What does a high ROUGE score typically indicate?

* A. The output uses a more diverse vocabulary than the reference text.
* B. The model-generated text has high n-gram overlap with the ground-truth human reference text.
* C. The model's training process has successfully converged to the lowest possible loss.
* D. The response passed prompt-safety filtering with high confidence.

Answer: 🟩 B. The model-generated text has high n-gram overlap with the ground-truth human reference text.

* Why it is correct: This is the strongest answer because B. The model-generated text has high n-gram overlap with the ground-truth human reference text is the prompt technique or output control that best addresses a scenario where an AI practitioner is reviewing a validation report for a fine-tuned technical support model. The report lists a metric called ROUGE (Recall-Oriented Understudy for Gisting Evaluation).
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 50 (Domain: Generative AI Models)
A developer wants to connect a local LLM to an internal database containing network telemetry logs. Instead of using a heavy vector database, they want the model to directly execute dynamically generated SQL commands to read the live database table securely. What security layer must sit between the model's output and the SQL engine?

* A. A standard prompt-injection classification model
* B. An isolated read-only query execution environment with strict input sanitization
* C. A continuous fine-tuning pipeline
* D. A symmetric network hardware token router

Answer: 🟩 B. An isolated read-only query execution environment with strict input sanitization

* Why it is correct: B. An isolated read-only query execution environment with strict input sanitization is the best answer because it is the core model or infrastructure concept that resolves a scenario where a developer wants to connect a local LLM to an internal database containing network telemetry logs. Instead of using a heavy vector database, they want the model to directly execute dynamically generated SQL commands to read the live database table securely.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 51 (Domain: Prompt Engineering)
A network engineer wants to secure an AI chat gateway from Indirect Prompt Injection. This threat occurs when an attacker hides malicious instructions inside an external asset that the AI reads while performing its duties. Which scenario represents this specific exploit vector?

* A. A user directly typing "ignore your rules" into the chat box.
* B. An attacker embedding an invisible prompt instruction into a public webpage that the AI scrapes during a RAG search.
* C. A rogue employee altering the model's training parameters on local servers.
* D. An API client overloading the gateway with millions of garbage tokens.

Answer: 🟩 B. An attacker embedding an invisible prompt instruction into a public webpage that the AI scrapes during a RAG search.

* Why it is correct: B. An attacker embedding an invisible prompt instruction into a public webpage that the AI scrapes during a RAG search. fits best here because the question is really about prompt structure and control, and that choice matches a scenario where a network engineer wants to secure an AI chat gateway from Indirect Prompt Injection. This threat occurs when an attacker hides malicious instructions inside an external asset that the AI reads while performing its duties.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 52 (Domain: Generative AI Models)
A data science team wants to adapt a massive foundational LLM to use a highly specific company acronym dictionary. They have limited compute budget and cannot afford to modify all 70 billion parameters of the base model. Which technique should they select to freeze the base weights and train a small, external adapter layer instead?

* A. Full Parameter Fine-Tuning
* B. Parameter-Efficient Fine-Tuning (PEFT) using LoRA
* C. Model Quantization down to 2-bit integers
* D. Context Window Padding

Answer: 🟩 B. Parameter-Efficient Fine-Tuning (PEFT) using LoRA

* Why it is correct: B. Parameter-Efficient Fine-Tuning (PEFT) using LoRA is correct because it matches the model, retrieval, hosting, or infrastructure concept required in a scenario where a data science team wants to adapt a massive foundational LLM to use a highly specific company acronym dictionary. They have limited compute budget and cannot afford to modify all 70 billion parameters of the base model.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 53 (Domain: Generative AI Models)
An automated code migration tool uses an LLM to rewrite obsolete Perl code into structured Python. The code migration pipeline frequently fails because the Perl scripts are larger than the model's native context window input limit. What design pattern resolves this technical bottleneck?

* A. Increasing model top-p parameters to 1.0
* B. Modularizing the code into functional blocks and processing them sequentially via a chunking workflow
* C. Lowering the model temperature configuration to 0
* D. Fine-tuning the model using unsupervised clustering datasets

Answer: 🟩 B. Modularizing the code into functional blocks and processing them sequentially via a chunking workflow

* Why it is correct: B. Modularizing the code into functional blocks and processing them sequentially via a chunking workflow is the best answer because it is the core model or infrastructure concept that resolves a scenario where an automated code migration tool uses an LLM to rewrite obsolete Perl code into structured Python. The code migration pipeline frequently fails because the Perl scripts are larger than the model's native context window input limit.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 54 (Domain: Agentic AI)
Why is a state management system (like a graph database or persistent memory state) critical in a multi-agent workflow that handles long-running software troubleshooting tasks?

* A. It speeds up the tokenization of raw text inputs.
* B. It allows multiple independent agents to read, update, and maintain a shared history of the problem context across execution steps.
* C. It converts model weights from 16-bit to 4-bit configurations dynamically.
* D. It ensures that the model operates deterministically without any temperature logic.

Answer: 🟩 B. It allows multiple independent agents to read, update, and maintain a shared history of the problem context across execution steps.

* Why it is correct: B. It allows multiple independent agents to read, update, and maintain a shared history of the problem context across execution steps. is the strongest answer because the scenario is testing agent control flow, and that option best reflects how the agent should behave.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 55 (Domain: Ethics and Security)
Which core principle of Cisco's Responsible AI framework guarantees that automated systems have visible lines of ownership, and that human operators retain the final authority to override or correct automated system actions?

* A. Scalability
* B. Accountability
* C. Contextuality
* D. Data Minimization

Answer: 🟩 B. Accountability

* Why it is correct: B. Accountability is correct because it is the safeguard or governance control that best addresses the core requirement in this question.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 56 (Domain: Agentic AI)
An engineering team is building a complex multi-agent system where a "Supervisor Agent" receives network trouble tickets and dynamically delegates sub-tasks to specialized worker agents (e.g., a BGP Expert Agent and an Interface Triage Agent). Which orchestration topology does this design represent?

* A. Linear Chain topology
* B. Hub-and-Spoke / Hierarchical routing topology
* C. Decentralized Peer-to-Peer mesh topology
* D. Static Parallel voting topology

Answer: 🟩 B. Hub-and-Spoke / Hierarchical routing topology

* Why it is correct: B. Hub-and-Spoke / Hierarchical routing topology is correct because it aligns with the planning, tool-use, memory, or orchestration behavior required in a scenario where an engineering team is building a complex multi-agent system where a "Supervisor Agent" receives network trouble tickets and dynamically delegates sub-tasks to specialized worker agents (e. g. , a BGP Expert Agent and an Interface Triage Agent).
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 57 (Domain: Agentic AI)
When building an enterprise RAG application using frameworks like LangChain or LlamaIndex, developers use a component called a Router. What is the primary operational task of a router within an orchestration pipeline?

* A. It maps network IP packets between VLAN boundaries.
* B. It dynamically analyzes an incoming user query and selects the most appropriate data tool or index to query (e.g., SQL database vs. Vector Vector store).
* C. It downscales model weight precision from FP16 to INT4 at runtime.
* D. It compiles Python syntax into machine code.

Answer: 🟩 B. It dynamically analyzes an incoming user query and selects the most appropriate data tool or index to query (e.g., SQL database vs. Vector Vector store).

* Why it is correct: B. It dynamically analyzes an incoming user query and selects the most appropriate data tool or index to query (e.g., SQL database vs. Vector Vector store). is the strongest answer because the scenario is testing agent control flow, and that option best reflects how the agent should behave.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 58 (Domain: Ethics and Security)
A medical software vendor wants to use a cloud-hosted LLM to summarize clinical notes. To comply with strict data privacy mandates (like HIPAA), they deploy a proxy system that scans outgoing prompts, identifies Protected Health Information (PHI) such as patient names or medical ID numbers, and replaces them with generic placeholders (e.g., [PATIENT_A]) before sending the text to the cloud API. What is this security practice called?

* A. Model Quantization
* B. Tokenization Layer Padding
* C. Data Pseudonymization / Masking
* D. Prompt Injection Interception

Answer: 🟩 C. Data Pseudonymization / Masking

* Why it is correct: C. Data Pseudonymization / Masking is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where a medical software vendor wants to use a cloud-hosted LLM to summarize clinical notes. To comply with strict data privacy mandates (like HIPAA), they deploy a proxy system that scans outgoing prompts, identifies Protected Health Information (PHI) such as patient names or medical ID numbers, and replaces them with generic placeholders (e. g. , [PATIENT_A]) before sending the text to the cloud API.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 59 (Domain: Prompt Engineering)
A developer wants an AI code assistant to refactor an asynchronous microservice written in Node.js. To get the highest quality refactored code block, the developer creates a prompt that outlines the exact performance constraints, details the structural layout of the current service, and appends a single comprehensive example of a perfectly refactored asynchronous pattern. What prompt methodology combination is being used?

* A. Zero-shot role shifting
* B. Context-augmented One-shot prompting
* C. Negative input filtering
* D. Speculative few-shot padding

Answer: 🟩 B. Context-augmented One-shot prompting

* Why it is correct: B. Context-augmented One-shot prompting is correct because it is the prompt design choice most likely to produce the requested behavior in a scenario where a developer wants an AI code assistant to refactor an asynchronous microservice written in Node. js. To get the highest quality refactored code block, the developer creates a prompt that outlines the exact performance constraints, details the structural layout of the current service, and appends a single comprehensive example of a perfectly refactored asynchronous pattern.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 60 (Domain: Generative AI Models)
An AI operations center is deploying an open-source model on a multi-GPU server. They choose to split the model's architectural layers sequentially across separate GPUs, meaning GPU 1 processes layers 1–24, and then passes its intermediate activation states over to GPU 2 to process layers 25–48. What specific form of distributed parallel compute does this setup describe?

* A. Tensor Parallelism (Intra-layer split)
* B. Pipeline Parallelism (Inter-layer split)
* C. Data Parallelism (Batch split)
* D. Fully Sharded Data Parallelism (FSDP)

Answer: 🟩 B. Pipeline Parallelism (Inter-layer split)

* Why it is correct: B. Pipeline Parallelism (Inter-layer split) fits most directly because the question is testing the underlying model, retrieval, hosting, or systems concept behind what specific form of distributed parallel compute does this setup describe.
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 61 (Domain: Agentic AI)
In an advanced multi-agent architecture, what is the main functional difference between a standard Stateless API tool call and a Stateful Agentic loop?

* A. API tools require physical network hardware tokens; agentic loops do not.
* B. API tools provide single-turn input/output execution, while agentic loops maintain continuous memory, self-reflect on tool outcomes, and run iteratively until a goal is achieved.
* C. API tools can only return integer outputs; agentic loops can handle text variables.
* D. Stateless API tools cannot be written in Python.

Answer: 🟩 B. API tools provide single-turn input/output execution, while agentic loops maintain continuous memory, self-reflect on tool outcomes, and run iteratively until a goal is achieved.

* Why it is correct: This scenario depends on agent behavior and control flow, and B. API tools provide single-turn input/output execution, while agentic loops maintain continuous memory, self-reflect on tool outcomes, and run iteratively until a goal is achieved is the option that best matches a scenario where in an advanced multi-agent architecture, what is the main functional difference between a standard Stateless API tool call and a Stateful Agentic loop.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 62 (Domain: Ethics and Security)
An organization suffers a data breach when a developer copies a secret corporate API key directly into a prompt while debugging an automation script on a public AI playground website. The public model provider uses those prompt logs for public retraining, exposing the key to external users. Which security pillar was compromised?

* A. Adversarial Evasion Resistance
* B. Multi-tenant Model Quantization
* C. Intellectual Property (IP) and Data Exfiltration Safeguards
* D. Algorithmic Explainability

Answer: 🟩 C. Intellectual Property (IP) and Data Exfiltration Safeguards

* Why it is correct: In a scenario where an organization suffers a data breach when a developer copies a secret corporate API key directly into a prompt while debugging an automation script on a public AI playground website. The public model provider uses those prompt logs for public retraining, exposing the key to external users, C. Intellectual Property (IP) and Data Exfiltration Safeguards is the strongest fit because it adds the safeguard or oversight the situation is missing.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 63 (Domain: Development and Workflow Automation)
An enterprise DevSecOps team configures an AI pipeline to auto-generate technical code comments for a legacy application. The team notices that while the text comments are highly coherent, the AI occasionally invents arbitrary framework features that don't exist in the project version. What evaluation metric should the team track to specifically measure the proportion of factual vs. hallucinated statements generated by the model?

* A. Latency-per-token (LPT)
* B. BLEU score
* C. Faithfulness / Groundedness score (via LLM-as-a-Judge)
* D. Context window footprint ratio

Answer: 🟩 C. Faithfulness / Groundedness score (via LLM-as-a-Judge)

* Why it is correct: C. Faithfulness / Groundedness score (via LLM-as-a-Judge) makes the most sense here because it matches the engineering constraint the scenario is actually testing.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 64 (Domain: Ethics and Security)
An engineer builds a network security agent that uses a Self-Correction design pattern. The agent generates a Cisco ASA firewall configuration template, runs an internal parsing validation tool on it, catches a syntax error, and then reads the error log to fix its own script before showing it to the human operator. Why is this loop fundamentally superior to a basic single-turn prompt execution?

* A. It completely bypasses the token serialization stage.
* B. It allows the system to autonomously catch and debug its own runtime logic errors before final delivery.
* C. It reduces the total operational cost to zero.
* D. It makes the model operate at 100% mathematical determinism without temperature controls.

Answer: 🟩 B. It allows the system to autonomously catch and debug its own runtime logic errors before final delivery.

* Why it is correct: B. It allows the system to autonomously catch and debug its own runtime logic errors before final delivery is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where an engineer builds a network security agent that uses a Self-Correction design pattern. The agent generates a Cisco ASA firewall configuration template, runs an internal parsing validation tool on it, catches a syntax error, and then reads the error log to fix its own script before showing it to the human operator.
* Why others are incorrect: The other options change the wrong prompt control, confuse prompting with another design choice, or would not reliably produce the requested behavior.

## Question 65 (Domain: Generative AI Models)
A developer wants to implement a advanced retrieval technique in LlamaIndex called Sentence Window Retrieval. How does this technique function to improve RAG performance?

* A. It restricts the LLM context window space to exactly one sentence at a time to reduce server billing.
* B. It retrieves a highly granular single-sentence chunk from the vector database for high similarity matching, but expands the text window to include surrounding sentences before passing it to the LLM context block.
* C. It converts all incoming words into standard cryptographic hashes.
* D. It uses a custom deep-learning router to skip text chunks completely.

Answer: 🟩 B. It retrieves a highly granular single-sentence chunk from the vector database for high similarity matching, but expands the text window to include surrounding sentences before passing it to the LLM context block.

* Why it is correct: B. It retrieves a highly granular single-sentence chunk from the vector database for high similarity matching, but expands the text window to include surrounding sentences before passing it to the LLM context block. is the best answer because it is the core model or infrastructure concept that resolves a scenario where a developer wants to implement a advanced retrieval technique in LlamaIndex called Sentence Window Retrieval.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 66 (Domain: Ethics and Security)
A company establishes a corporate "System Registry" where all internal AI agents must be explicitly cataloged along with their approved API scopes, target actions, and data access clearings. Which core corporate governance objective is fulfilled by establishing this registry?

* A. Maximizing speculative decoding velocity
* B. Asset Tracking, Visibility, and Risk Management across the AI landscape
* C. Eliminating the necessity for vector data embeddings
* D. Automating model bit-width downscaling pipelines

Answer: 🟩 B. Asset Tracking, Visibility, and Risk Management across the AI landscape

* Why it is correct: B. Asset Tracking, Visibility, and Risk Management across the AI landscape is the best answer because the scenario turns on a security, privacy, or governance control, and this option is the one that directly provides it.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 67 (Domain: Generative AI Models)
When deploying LLM inference clusters at scale, what is the primary architectural limitation or disadvantage of using an extreme quantization approach, such as downscaling an open-source model's weights all the way from FP16 to 2-bit precision (INT2)?

* A. The model's VRAM footprint scales up by four times.
* B. Severe degradation of model reasoning capabilities, language syntax coherence, and overall intelligence metrics.
* C. Complete loss of compatibility with standard Python runtimes.
* D. Extreme increases in physical power consumption across the GPU chassis.

Answer: 🟩 B. Severe degradation of model reasoning capabilities, language syntax coherence, and overall intelligence metrics.

* Why it is correct: B. Severe degradation of model reasoning capabilities, language syntax coherence, and overall intelligence metrics is correct because it matches the model, retrieval, hosting, or infrastructure concept required in when deploying LLM inference clusters at scale, what is the primary architectural limitation or disadvantage of using an extreme quantization approach, such as downscaling an open-source model's weights all the way from FP16 to 2-bit precision (INT2).
* Why others are incorrect: The other options point to different model or infrastructure ideas, but they do not satisfy the specific technical requirement in the prompt.

## Question 68 (Domain: Generative AI Models)
A DevOps team wants to use an AI engine to auto-generate comprehensive infrastructure blueprints for global enterprise data centers using JSON formats. To guarantee that the model outputs are programmatically parseable by downstream orchestrators without any conversational chatter, which setup should the team deploy?

* A. Setting temperature to 1.0 and widening the top-p filter pool
* B. Enabling an explicit JSON Schema constraint in the model inference API parameters (Structured Outputs)
* C. Passing the entire document database into a context window padding array
* D. Forcing the model to run inside an offline Pipeline Parallel cluster

Answer: 🟩 B. Enabling an explicit JSON Schema constraint in the model inference API parameters (Structured Outputs)

* Why it is correct: B. Enabling an explicit JSON Schema constraint in the model inference API parameters (Structured Outputs) is the best answer because it is the core model or infrastructure concept that resolves a scenario where a DevOps team wants to use an AI engine to auto-generate comprehensive infrastructure blueprints for global enterprise data centers using JSON formats.
* Why others are incorrect: The other options point to different engineering activities or unrealistic guarantees, but they do not solve the workflow constraint in the prompt.

## Question 69 (Domain: Agentic AI)
Within enterprise search architectures, what is the core functional relationship between a Retriever and a Reranker model?

* A. The retriever downscales data weights, and the reranker trains new parameter vectors.
* B. The retriever acts as a stateful agent, and the reranker functions as a stateless token parser.
* C. The retriever runs a fast, coarse semantic search to fetch a large pool of candidate documents (e.g., top 50), and the reranker runs a slower, high-precision deep learning calculation to sort those 50 documents into the absolute best order for the LLM.
* D. The retriever parses raw PDF documents, and the reranker builds SQL schemas.

Answer: 🟩 C. The retriever runs a fast, coarse semantic search to fetch a large pool of candidate documents (e.g., top 50), and the reranker runs a slower, high-precision deep learning calculation to sort those 50 documents into the absolute best order for the LLM.

* Why it is correct: C. The retriever runs a fast, coarse semantic search to fetch a large pool of candidate documents (e.g., top 50), and the reranker runs a slower, high-precision deep learning calculation to sort those 50 documents into the absolute best order for the LLM. is the strongest answer because the scenario is testing agent control flow, and that option best reflects how the agent should behave.
* Why others are incorrect: The other options reference adjacent ideas, but they do not match the agent behavior, tool flow, or orchestration pattern being tested here.

## Question 70 (Domain: Ethics and Security)
A networking company is implementing an AI system to triage employee security clearances. Under standard algorithmic fairness and ethical AI frameworks, why must the engineering team explicitly avoid using a "black-box" deep learning model that offers zero explainability features for this specific task?

* A. Unexplainable models require three times more network switches to run.
* B. It prevents affected employees from understanding or auditing the logical justification behind a high-risk access rejection, violating corporate transparency and accountability guidelines.
* C. Black-box models automatically broadcast prompt inputs to public internet search indexes.
* D. Internal IT teams cannot verify token usage pricing scales without explainability matrices.

Answer: 🟩 B. It prevents affected employees from understanding or auditing the logical justification behind a high-risk access rejection, violating corporate transparency and accountability guidelines.

* Why it is correct: B. It prevents affected employees from understanding or auditing the logical justification behind a high-risk access rejection, violating corporate transparency and accountability guidelines is correct because it is the control that best addresses the governance, safety, privacy, or fairness requirement in a scenario where a networking company is implementing an AI system to triage employee security clearances.
* Why others are incorrect: The other options do not add the governance, privacy, security, or fairness safeguard that this scenario requires.

## Question 71 (Domain: Generative AI Models)
An architect is choosing between a managed cloud LLM API and a locally hosted open-source model for an internal support assistant that handles regulated customer records. Which consideration most strongly favors local deployment?

* A. Lower need for prompt engineering because local models are more deterministic
* B. Greater control over data privacy, residency, and network boundaries
* C. Automatic access to the broadest model marketplace without vendor agreements
* D. Elimination of all infrastructure management and observability requirements

Answer: 🟩 B. Greater control over data privacy, residency, and network boundaries

* Why it is correct: Greater control over data privacy, residency, and network boundaries is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 72 (Domain: Generative AI Models)
A support engineer sends an incident bundle whose total tokens exceed the model's context window. What is the most likely technical implication?

* A. The model automatically creates a fine-tuned adapter for the overflow content
* B. The application must chunk, summarize, or otherwise reduce the input before full processing
* C. The model reclassifies the request as an embedding job and bypasses generation
* D. The provider caches the entire overflow and resumes generation in a second hidden pass

Answer: 🟩 B. The application must chunk, summarize, or otherwise reduce the input before full processing

* Why it is correct: The application must chunk, summarize, or otherwise reduce the input before full processing is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 73 (Domain: Generative AI Models)
An engineer needs a single model workflow that can inspect UI screenshots, reason over network diagrams, and answer follow-up questions based on short text prompts. What capability is most appropriate?

* A. Multimodal reasoning across text and visual inputs
* B. Static token compression with deterministic decoding
* C. Symbolic parsing with no latent representation layer
* D. Single-label classification with no conversational state

Answer: 🟩 A. Multimodal reasoning across text and visual inputs

* Why it is correct: Multimodal reasoning across text and visual inputs is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 74 (Domain: Generative AI Models)
In a Retrieval-Augmented Generation workflow, what is the primary role of document embeddings?

* A. To compress the source corpus into a smaller archival format
* B. To represent content numerically so semantically related items can be retrieved by similarity
* C. To convert natural language into executable database transactions
* D. To force the LLM to cite only one chunk per answer

Answer: 🟩 B. To represent content numerically so semantically related items can be retrieved by similarity

* Why it is correct: To represent content numerically so semantically related items can be retrieved by similarity is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 75 (Domain: Generative AI Models)
Why might a team intentionally select a smaller specialized model from a model hub instead of the largest general-purpose model available?

* A. Smaller models inherently remove hallucinations on narrow tasks
* B. They may provide a better tradeoff among latency, cost, and fit for the target workload
* C. Larger models cannot support multimodal inputs in production
* D. Specialized models eliminate the need for retrieval and guardrails entirely

Answer: 🟩 B. They may provide a better tradeoff among latency, cost, and fit for the target workload

* Why it is correct: They may provide a better tradeoff among latency, cost, and fit for the target workload is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 76 (Domain: Ethics and Security)
A team is comparing two deployment options for a helpdesk summarization model. The cloud API is cheaper to start with and scales easily, while the local deployment offers lower data-exposure risk but higher operational overhead. What tradeoff is being evaluated most directly?

* A. Hosting tradeoffs across cost, privacy, latency, and scalability
* B. Model distillation versus reinforcement learning tradeoffs
* C. Fine-tuning versus pretraining tradeoffs
* D. Batch inference versus speculative decoding tradeoffs

Answer: 🟩 A. Hosting tradeoffs across cost, privacy, latency, and scalability

* Why it is correct: Hosting tradeoffs across cost, privacy, latency, and scalability is correct because it best matches the model capability, hosting, context-window, or retrieval concept described in the scenario.
* Why others are incorrect: The other options either confuse adjacent infrastructure or model concepts, overstate guarantees, or solve a different problem than the one in the prompt.

## Question 77 (Domain: Prompt Engineering)
Which prompt pattern most directly establishes the model's role, the task boundaries, and the required output format before work begins?

* A. Roles, instructions, and constraints
* B. Chunking, overlap, and reranking
* C. Quantization, pruning, and caching
* D. Embedding, indexing, and routing

Answer: 🟩 A. Roles, instructions, and constraints

* Why it is correct: Roles, instructions, and constraints is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 78 (Domain: Prompt Engineering)
An operator refines a model prompt over three cycles by comparing outputs, clarifying requirements, and correcting ambiguity after each attempt. Which prompting approach is this?

* A. Iterative prompting
* B. Zero-shot prompting
* C. Gradient-based fine-tuning
* D. Knowledge distillation

Answer: 🟩 A. Iterative prompting

* Why it is correct: Iterative prompting is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 79 (Domain: Prompt Engineering)
Why is it useful to specify attributes such as composition, lighting, and style when prompting an image-generation model?

* A. Because visual generation benefits from explicit constraints on non-text output attributes
* B. Because the model otherwise disables its safety filters automatically
* C. Because image prompts must include SQL-like syntax to be valid
* D. Because detailed prompts force the provider to retain no logs

Answer: 🟩 B. Because the model otherwise disables its safety filters automatically

* Why it is correct: Because the model otherwise disables its safety filters automatically is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 80 (Domain: Prompt Engineering)
Which scenario best represents an indirect prompt injection attack?

* A. A user enters a direct jailbreak attempt into the same chat session
* B. A hostile instruction is hidden in a retrieved webpage that the assistant later treats as source content
* C. A developer lowers the temperature in a production workflow
* D. A provider rotates API credentials during a maintenance window

Answer: 🟩 B. A hostile instruction is hidden in a retrieved webpage that the assistant later treats as source content

* Why it is correct: A hostile instruction is hidden in a retrieved webpage that the assistant later treats as source content is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 81 (Domain: Ethics and Security)
Which responsible AI principle is most directly reinforced when an organization defines clear ownership for an AI system and specifies who can override its decisions?

* A. Accountability
* B. Compression efficiency
* C. Context persistence
* D. Output diversity

Answer: 🟩 A. Accountability

* Why it is correct: Accountability is correct because it best matches the governance, privacy, fairness, or risk-control principle that directly addresses the scenario.
* Why others are incorrect: The other options touch adjacent AI concerns, but they do not address the specific governance or security control failure described in the question.

## Question 82 (Domain: Ethics and Security)
A company removes customer names, account numbers, and phone numbers from support transcripts before sending them to a third-party LLM provider. What control objective is this most directly supporting?

* A. Data privacy and exposure reduction
* B. Response determinism and reproducibility
* C. Long-context summarization quality
* D. Autonomous agent planning accuracy

Answer: 🟩 A. Data privacy and exposure reduction

* Why it is correct: Data privacy and exposure reduction is correct because it best matches the governance, privacy, fairness, or risk-control principle that directly addresses the scenario.
* Why others are incorrect: The other options touch adjacent AI concerns, but they do not address the specific governance or security control failure described in the question.

## Question 83 (Domain: Ethics and Security)
Why is AI-generated misinformation treated as a governance concern rather than only a model-quality issue?

* A. Because plausible but false content can spread quickly and influence downstream decisions or users
* B. Because misinformation only occurs in locally hosted models with small context windows
* C. Because governance teams are responsible for GPU procurement and capacity planning
* D. Because misinformation automatically implies a data breach has already occurred

Answer: 🟩 A. Because plausible but false content can spread quickly and influence downstream decisions or users

* Why it is correct: Because plausible but false content can spread quickly and influence downstream decisions or users is correct because it best matches the governance, privacy, fairness, or risk-control principle that directly addresses the scenario.
* Why others are incorrect: The other options touch adjacent AI concerns, but they do not address the specific governance or security control failure described in the question.

## Question 84 (Domain: Ethics and Security)
Which activity best demonstrates enterprise AI governance in practice?

* A. Maintaining policies, approvals, inventories, and risk reviews for AI use cases
* B. Disabling model logs to reduce storage cost
* C. Increasing temperature for creative exploration in regulated workflows
* D. Replacing all human review with final-answer automation

Answer: 🟩 A. Maintaining policies, approvals, inventories, and risk reviews for AI use cases

* Why it is correct: Maintaining policies, approvals, inventories, and risk reviews for AI use cases is correct because it best matches the governance, privacy, fairness, or risk-control principle that directly addresses the scenario.
* Why others are incorrect: The other options touch adjacent AI concerns, but they do not address the specific governance or security control failure described in the question.

## Question 85 (Domain: Ethics and Security)
An internal model makes systematically worse recommendations for a subgroup because the training data underrepresents them. Which risk does this illustrate most directly?

* A. Bias caused by unrepresentative training data
* B. Hallucination caused by insufficient context-window length
* C. Prompt leakage caused by missing output truncation
* D. Overfitting caused by vector-database corruption

Answer: 🟩 A. Bias caused by unrepresentative training data

* Why it is correct: Bias caused by unrepresentative training data is correct because it best matches the governance, privacy, fairness, or risk-control principle that directly addresses the scenario.
* Why others are incorrect: The other options touch adjacent AI concerns, but they do not address the specific governance or security control failure described in the question.

## Question 86 (Domain: Data Research and Analysis)
How can AI most usefully assist during exploratory data analysis (EDA)?

* A. By surfacing candidate patterns, anomalies, and follow-up questions for analysts to verify
* B. By proving causal relationships without any statistical testing
* C. By replacing the need for raw-data inspection and source validation
* D. By guaranteeing that generated summaries are free from unsupported inference

Answer: 🟩 A. By surfacing candidate patterns, anomalies, and follow-up questions for analysts to verify

* Why it is correct: By surfacing candidate patterns, anomalies, and follow-up questions for analysts to verify is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 87 (Domain: Data Research and Analysis)
Which task is the clearest example of AI-assisted automated data preparation?

* A. Detecting missing values and standardizing inconsistent field formats across source files
* B. Expanding a model's context window for longer prompts
* C. Provisioning GPUs for inference autoscaling
* D. Registering external tools through MCP

Answer: 🟩 A. Detecting missing values and standardizing inconsistent field formats across source files

* Why it is correct: Detecting missing values and standardizing inconsistent field formats across source files is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 88 (Domain: Ethics and Security)
When using AI to analyze sensitive enterprise datasets, which safeguard is most important before analysis begins?

* A. Applying access controls, masking, or similar exposure-prevention measures to sensitive fields
* B. Increasing output temperature so summaries become less repetitive
* C. Eliminating audit logs to reduce retention risk
* D. Converting every dataset into image form before processing

Answer: 🟩 A. Applying access controls, masking, or similar exposure-prevention measures to sensitive fields

* Why it is correct: Applying access controls, masking, or similar exposure-prevention measures to sensitive fields is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 89 (Domain: Development and Workflow Automation)
Which example best shows AI contributing across the software development lifecycle rather than only during coding?

* A. Using AI for requirement drafting, prototype scaffolding, test support, and deployment readiness checks
* B. Using AI only after deployment to create release announcements
* C. Restricting AI to spreadsheet formatting for sprint metrics
* D. Replacing version control with prompt history and chat logs

Answer: 🟩 A. Using AI for requirement drafting, prototype scaffolding, test support, and deployment readiness checks

* Why it is correct: Using AI for requirement drafting, prototype scaffolding, test support, and deployment readiness checks is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 90 (Domain: Development and Workflow Automation)
What is a realistic advantage of AI-assisted code generation during prototyping?

* A. It accelerates first-pass scaffolding that engineers can then review and refine
* B. It guarantees production-safe code without testing or security review
* C. It removes the need to understand dependencies and runtime behavior
* D. It prevents all integration regressions by design

Answer: 🟩 A. It accelerates first-pass scaffolding that engineers can then review and refine

* Why it is correct: It accelerates first-pass scaffolding that engineers can then review and refine is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 91 (Domain: Development and Workflow Automation)
Why is workflow monitoring important in AI-powered automation pipelines?

* A. It helps teams detect output-quality issues, failures, latency changes, and cost drift over time
* B. It removes the need for logging and operational observability
* C. It guarantees that retrieval pipelines never return irrelevant documents
* D. It prevents agents from needing tool permissions or approval workflows

Answer: 🟩 A. It helps teams detect output-quality issues, failures, latency changes, and cost drift over time

* Why it is correct: It helps teams detect output-quality issues, failures, latency changes, and cost drift over time is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 92 (Domain: Development and Workflow Automation)
How does poor context-window management typically affect rapid prototyping workflows?

* A. It can increase cost and latency while also causing relevant context to be dropped or over-compressed
* B. It guarantees lower cost because more context improves caching ratios automatically
* C. It only matters for multimodal image-generation systems
* D. It forces the provider to fine-tune the model on each request

Answer: 🟩 A. It can increase cost and latency while also causing relevant context to be dropped or over-compressed

* Why it is correct: It can increase cost and latency while also causing relevant context to be dropped or over-compressed is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 93 (Domain: Development and Workflow Automation)
Which scenario best reflects AI improving code quality rather than merely generating code?

* A. The assistant explains a stack trace, identifies a likely null-handling bug, and proposes a targeted patch
* B. The assistant creates a README title and project tagline for a demo repository
* C. The assistant compresses build artifacts before publication
* D. The assistant changes branch names to fit a team convention

Answer: 🟩 A. The assistant explains a stack trace, identifies a likely null-handling bug, and proposes a targeted patch

* Why it is correct: The assistant explains a stack trace, identifies a likely null-handling bug, and proposes a targeted patch is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 94 (Domain: Development and Workflow Automation)
What is the clearest example of AI assisting during testing rather than during implementation?

* A. Generating boundary-case unit test ideas from an API contract
* B. Rewriting a product requirement into a user story
* C. Summarizing a design meeting into action items
* D. Converting a diagram into a presentation slide deck

Answer: 🟩 A. Generating boundary-case unit test ideas from an API contract

* Why it is correct: Generating boundary-case unit test ideas from an API contract is correct because it best matches the software-delivery or workflow-automation practice most directly tied to the scenario.
* Why others are incorrect: The other options describe adjacent engineering activities, unrealistic guarantees, or actions that do not improve the workflow in the way the question asks.

## Question 95 (Domain: Agentic AI)
What most clearly differentiates an agentic AI workflow from a basic generative AI chatbot?

* A. The agent can plan, invoke tools, track state, and iterate toward a goal across multiple steps
* B. The agent always produces shorter outputs because it compresses reasoning into embeddings
* C. The agent is limited to static FAQ responses to preserve determinism
* D. The agent cannot interact with systems outside the initial prompt context

Answer: 🟩 A. The agent can plan, invoke tools, track state, and iterate toward a goal across multiple steps

* Why it is correct: The agent can plan, invoke tools, track state, and iterate toward a goal across multiple steps is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 96 (Domain: Prompt Engineering)
Which design principle is central to a capable autonomous AI agent?

* A. Planning, acting, observing outcomes, and adapting subsequent steps
* B. Removing all tool feedback so the agent relies only on prior training
* C. Fixing the output format and prohibiting all branching behavior
* D. Restricting the agent to one-turn generation without memory or state

Answer: 🟩 A. Planning, acting, observing outcomes, and adapting subsequent steps

* Why it is correct: Planning, acting, observing outcomes, and adapting subsequent steps is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 97 (Domain: Agentic AI)
In the context of agentic AI, what is the practical purpose of Model Context Protocol (MCP) primitives?

* A. They standardize how models and agents connect to tools, resources, and reusable prompt surfaces
* B. They compress neural network weights so larger models fit into local VRAM
* C. They replace approval workflows by turning agent output into implicit authorization
* D. They ensure retrieval systems never need embeddings or vector indexes

Answer: 🟩 A. They standardize how models and agents connect to tools, resources, and reusable prompt surfaces

* Why it is correct: They standardize how models and agents connect to tools, resources, and reusable prompt surfaces is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 98 (Domain: Ethics and Security)
Why is human-in-the-loop (HITL) especially important for agentic workflows that can change operational systems?

* A. It inserts approval or correction checkpoints before high-impact actions are finalized
* B. It guarantees that the agent no longer needs observability data or audit logs
* C. It removes the need for least-privilege permissions on connected tools
* D. It prevents the agent from using structured outputs or schemas

Answer: 🟩 A. It inserts approval or correction checkpoints before high-impact actions are finalized

* Why it is correct: It inserts approval or correction checkpoints before high-impact actions are finalized is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 99 (Domain: Generative AI Models)
An agent collects asset information from one system, converts fields to match a CRM schema, and sends the transformed payload to a case-management API. Which capability is it exercising most directly?

* A. Data transformation and mapping across systems
* B. Prompt injection through cross-domain retrieval
* C. Model quantization for lower inference cost
* D. Context-window extension through recursive chunking

Answer: 🟩 A. Data transformation and mapping across systems

* Why it is correct: Data transformation and mapping across systems is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 100 (Domain: Agentic AI)
Which scenario best demonstrates orchestration in an agentic AI system?

* A. A supervisor agent delegates subtasks to specialized workers and combines their outputs into a final result
* B. A model returns a single standalone summary from one prompt with no tool usage
* C. A vector database stores embeddings for later retrieval by a separate application
* D. A fine-tuning job updates weights using labeled examples offline

Answer: 🟩 A. A supervisor agent delegates subtasks to specialized workers and combines their outputs into a final result

* Why it is correct: A supervisor agent delegates subtasks to specialized workers and combines their outputs into a final result is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 101 (Domain: Generative AI Models)
An operations engineer wants an LLM to produce only a JSON object containing `severity`, `owner`, and `next_step` fields for incident triage. Which prompt addition best reinforces that requirement before any downstream validation occurs?

* A. A role declaration plus an explicit schema-like output constraint
* B. A request to "be creative but concise" in the final answer
* C. A note that the user prefers short paragraphs over bullet points
* D. A temperature increase to improve structural consistency

Answer: 🟩 A. A role declaration plus an explicit schema-like output constraint

* Why it is correct: A role declaration plus an explicit schema-like output constraint is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 102 (Domain: Generative AI Models)
A team is improving an internal assistant by adding three high-quality examples showing how raw alerts should be rewritten into executive-friendly summaries. Which prompting technique is this?

* A. Few-shot prompting
* B. Reinforcement learning
* C. Distillation
* D. Quantization-aware tuning

Answer: 🟩 A. Few-shot prompting

* Why it is correct: Few-shot prompting is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 103 (Domain: Prompt Engineering)
What is the main benefit of chained prompting in a complex workflow?

* A. It decomposes a task into intermediate steps that can be checked or refined separately
* B. It guarantees lower token usage than a single prompt in every case
* C. It removes the need for retrieval or external context
* D. It forces the model to reveal hidden reasoning tokens to the application

Answer: 🟩 A. It decomposes a task into intermediate steps that can be checked or refined separately

* Why it is correct: It decomposes a task into intermediate steps that can be checked or refined separately is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 104 (Domain: Generative AI Models)
An audio-generation workflow keeps producing outputs with the correct topic but the wrong style. What is the most appropriate prompt adjustment?

* A. Add explicit style and format constraints for the target audio output
* B. Remove all constraints so the model has more freedom to generalize
* C. Replace the prompt with a vector embedding and skip generation
* D. Lower the API timeout to encourage concise results

Answer: 🟩 A. Add explicit style and format constraints for the target audio output

* Why it is correct: Add explicit style and format constraints for the target audio output is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 105 (Domain: Prompt Engineering)
Which scenario is the clearest example of a direct prompt injection attempt?

* A. A user explicitly instructs the assistant to ignore prior rules and expose hidden instructions
* B. A malicious sentence is embedded in a PDF later indexed for retrieval
* C. A reranker incorrectly promotes a weak passage to the top of the candidate set
* D. A developer caches a system prompt for repeated requests

Answer: 🟩 A. A user explicitly instructs the assistant to ignore prior rules and expose hidden instructions

* Why it is correct: A user explicitly instructs the assistant to ignore prior rules and expose hidden instructions is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 106 (Domain: Generative AI Models)
Why is it risky to let retrieved documents overwrite the authority of system-level instructions in an enterprise assistant?

* A. It increases the chance that untrusted retrieved content can steer the model's behavior
* B. It guarantees that output length will exceed the model context window
* C. It prevents the use of vector databases for future requests
* D. It disables JSON-mode responses at the API layer

Answer: 🟩 A. It increases the chance that untrusted retrieved content can steer the model's behavior

* Why it is correct: It increases the chance that untrusted retrieved content can steer the model's behavior is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 107 (Domain: Generative AI Models)
Which mitigation is strongest when an LLM sometimes fabricates unsupported answers instead of acknowledging missing evidence?

* A. Instruct the model to answer only from supplied evidence and explicitly state when evidence is insufficient
* B. Increase output token limits so the model has more room to speculate
* C. Remove the system prompt entirely to reduce instruction conflict
* D. Replace all retrieved context with one short summary paragraph

Answer: 🟩 A. Instruct the model to answer only from supplied evidence and explicitly state when evidence is insufficient

* Why it is correct: Instruct the model to answer only from supplied evidence and explicitly state when evidence is insufficient is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 108 (Domain: Prompt Engineering)
What is the clearest sign that a prompt is under-specified?

* A. Different runs produce structurally inconsistent outputs for the same business task
* B. The provider returns usage metrics after each request
* C. The application stores responses in a database
* D. The model supports multimodal inputs

Answer: 🟩 A. Different runs produce structurally inconsistent outputs for the same business task

* Why it is correct: Different runs produce structurally inconsistent outputs for the same business task is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 109 (Domain: Generative AI Models)
An engineer wants a model to extract facts first, decide second, and justify third. Which prompt structure aligns best with that objective?

* A. A staged prompt sequence with distinct intermediate tasks
* B. A single high-temperature request with no formatting constraints
* C. A retrieval-free completion with maximum randomness
* D. A model-hosting switch from cloud to local deployment

Answer: 🟩 A. A staged prompt sequence with distinct intermediate tasks

* Why it is correct: A staged prompt sequence with distinct intermediate tasks is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 110 (Domain: Prompt Engineering)
Which adjustment most directly improves reproducibility for a prompt-based classification workflow?

* A. Lower the temperature and tighten label constraints
* B. Increase the top-p range and remove examples
* C. Expand the prompt with unrelated background context
* D. Rotate the prompt wording on each request

Answer: 🟩 A. Lower the temperature and tighten label constraints

* Why it is correct: Lower the temperature and tighten label constraints is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 111 (Domain: Generative AI Models)
Why can overloading a prompt with loosely related background material reduce answer quality?

* A. It can dilute relevant instructions and consume limited context-window space
* B. It forces the provider to retrain the base model for every call
* C. It disables safety policies applied at the API gateway
* D. It converts the task into a multimodal inference request automatically

Answer: 🟩 A. It can dilute relevant instructions and consume limited context-window space

* Why it is correct: It can dilute relevant instructions and consume limited context-window space is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 112 (Domain: Ethics and Security)
Which output control is most appropriate when an application must parse the model response programmatically?

* A. Structured output constraints backed by validation
* B. Free-form narrative generation with a high creativity setting
* C. Allowing the model to choose any convenient format
* D. Removing all schema references from the prompt

Answer: 🟩 A. Structured output constraints backed by validation

* Why it is correct: Structured output constraints backed by validation is correct because it best matches the prompt-design or grounding technique that most directly shapes the model behavior the question is asking about.
* Why others are incorrect: The other options may sound related, but they either adjust the wrong control, confuse prompting with architecture changes, or weaken the intended instruction pattern.

## Question 113 (Domain: Data Research and Analysis)
An analyst is preparing incident data from three systems for a trend report. Device identifiers use different formats, timestamps are in mixed time zones, and some severity fields are blank. Which action should happen before drawing conclusions from the data?

* A. Normalize identifiers, standardize timestamps, and resolve or flag missing values
* B. Increase the model temperature so inconsistent records are generalized automatically
* C. Fine-tune a summarization model before checking data quality issues
* D. Delete all rows with missing fields without first measuring the impact on coverage

Answer: 🟩 A. Normalize identifiers, standardize timestamps, and resolve or flag missing values

* Why it is correct: Normalize identifiers, standardize timestamps, and resolve or flag missing values is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 114 (Domain: Data Research and Analysis)
A team wants to compare how often a support assistant hallucinated across two different prompt versions. What is the most defensible way to do that?

* A. Evaluate both versions against the same labeled test set and scoring rubric
* B. Let different reviewers use different criteria for each version to capture more perspectives
* C. Compare only the longest responses because longer answers contain more evidence
* D. Ask the model which version it believes is safer and accept its answer as the benchmark

Answer: 🟩 A. Evaluate both versions against the same labeled test set and scoring rubric

* Why it is correct: Evaluate both versions against the same labeled test set and scoring rubric is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 115 (Domain: Data Research and Analysis)
When building a retrieval corpus from internal manuals, what chunking outcome most directly improves downstream analysis quality?

* A. Chunks preserve enough local context to remain meaningful while staying small enough for precise retrieval
* B. Chunks are made as large as possible so the retriever always returns full documents
* C. Chunks are grouped by file size rather than topic to simplify storage accounting
* D. Chunks remove headings and structure so all passages look statistically similar

Answer: 🟩 A. Chunks preserve enough local context to remain meaningful while staying small enough for precise retrieval

* Why it is correct: Chunks preserve enough local context to remain meaningful while staying small enough for precise retrieval is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 116 (Domain: Agentic AI)
What is the clearest difference between a tool-using agent and a standard one-shot text generation workflow?

* A. The agent can observe outcomes from tool calls and adjust its next action accordingly
* B. The agent always returns shorter outputs than a one-shot workflow
* C. The agent cannot be governed by policy because it is autonomous
* D. The agent never needs external context once deployed

Answer: 🟩 A. The agent can observe outcomes from tool calls and adjust its next action accordingly

* Why it is correct: The agent can observe outcomes from tool calls and adjust its next action accordingly is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 117 (Domain: Agentic AI)
Why is state management important in multi-step agentic workflows?

* A. It allows the agent to track prior actions, intermediate results, and unresolved objectives
* B. It guarantees that every tool returns valid JSON without validation
* C. It removes the need for prompt design in later steps
* D. It prevents retrieval systems from returning stale documents

Answer: 🟩 A. It allows the agent to track prior actions, intermediate results, and unresolved objectives

* Why it is correct: It allows the agent to track prior actions, intermediate results, and unresolved objectives is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 118 (Domain: Agentic AI)
Which architecture most clearly demonstrates orchestration rather than simple generation?

* A. A supervisor agent routes subtasks to specialist agents and merges their outputs
* B. A single LLM rewrites a paragraph into a shorter paragraph
* C. A vector store returns the top three similar passages for a query
* D. A fine-tuned model classifies tickets into one of four labels

Answer: 🟩 A. A supervisor agent routes subtasks to specialist agents and merges their outputs

* Why it is correct: A supervisor agent routes subtasks to specialist agents and merges their outputs is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 119 (Domain: Agentic AI)
In practical terms, what value does MCP provide to an agentic ecosystem?

* A. A standardized interface for exposing tools, resources, and prompt surfaces to models and agents
* B. A new neural architecture for reducing hallucinations through weight sharing
* C. A compression layer that reduces token billing during long sessions
* D. A retrieval algorithm that replaces embeddings with deterministic indexes

Answer: 🟩 A. A standardized interface for exposing tools, resources, and prompt surfaces to models and agents

* Why it is correct: A standardized interface for exposing tools, resources, and prompt surfaces to models and agents is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 120 (Domain: Ethics and Security)
Why should high-impact agent actions usually require explicit human approval?

* A. Because autonomous action can create operational or compliance risk if the agent is wrong
* B. Because HITL eliminates the need for audit logging and observability
* C. Because agents are unable to process structured data safely without human rewriting
* D. Because approval steps increase model creativity under constrained prompts

Answer: 🟩 A. Because autonomous action can create operational or compliance risk if the agent is wrong

* Why it is correct: Because autonomous action can create operational or compliance risk if the agent is wrong is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 121 (Domain: Prompt Engineering)
An agent reads a CSV export, maps several columns to a CRM API schema, and submits the transformed payload. Which agent capability is most directly demonstrated?

* A. Data transformation and mapping across systems
* B. Prompt injection detection through content hashing
* C. Context-window extension through recursive summarization
* D. Model compression through low-rank adaptation

Answer: 🟩 A. Data transformation and mapping across systems

* Why it is correct: Data transformation and mapping across systems is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 122 (Domain: Ethics and Security)
What is the main benefit of giving an agent access to specialized tools instead of expecting the base model to answer everything from its prior training?

* A. Tools let the agent fetch current data, execute actions, or validate results externally
* B. Tools eliminate all need for prompts, instructions, or constraints
* C. Tools guarantee that the agent will never hallucinate
* D. Tools replace the need for permissions and access control

Answer: 🟩 A. Tools let the agent fetch current data, execute actions, or validate results externally

* Why it is correct: Tools let the agent fetch current data, execute actions, or validate results externally is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 123 (Domain: Agentic AI)
Which example best illustrates observation inside a ReAct-style agent loop?

* A. The agent receives a tool result and uses it to decide the next step
* B. The agent hides all intermediate outputs from its own controller
* C. The agent compresses model weights after each reasoning step
* D. The agent converts every response to embeddings before returning text

Answer: 🟩 A. The agent receives a tool result and uses it to decide the next step

* Why it is correct: The agent receives a tool result and uses it to decide the next step is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 124 (Domain: Agentic AI)
Why is least-privilege access important for connected agent tools?

* A. It limits the blast radius if the agent misuses a tool or receives malicious instructions
* B. It ensures the agent can skip human approvals for privileged tasks
* C. It makes context windows effectively unlimited for sensitive workflows
* D. It allows the agent to self-certify compliance automatically

Answer: 🟩 A. It limits the blast radius if the agent misuses a tool or receives malicious instructions

* Why it is correct: It limits the blast radius if the agent misuses a tool or receives malicious instructions is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 125 (Domain: Generative AI Models)
Which failure mode is most likely when an agent lacks strong schema validation between tools?

* A. Downstream systems receive malformed or semantically mismapped payloads
* B. The model loses its pretraining weights during inference
* C. The provider disables all external API access permanently
* D. The context window becomes larger than the model supports

Answer: 🟩 A. Downstream systems receive malformed or semantically mismapped payloads

* Why it is correct: Downstream systems receive malformed or semantically mismapped payloads is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 126 (Domain: Agentic AI)
What is the clearest reason to separate a planning role from an execution role in an agentic system?

* A. It improves control and observability by distinguishing decision-making from action-taking
* B. It guarantees lower token cost than any single-agent design
* C. It prevents the need for retrieval, embeddings, or context management
* D. It ensures every output is deterministic without parameter tuning

Answer: 🟩 A. It improves control and observability by distinguishing decision-making from action-taking

* Why it is correct: It improves control and observability by distinguishing decision-making from action-taking is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 127 (Domain: Agentic AI)
An agent proposes a firewall change, runs a policy-check tool, sees a failure, and revises the plan before requesting approval. What does this best demonstrate?

* A. A feedback loop using tool observations to refine behavior before final action
* B. A single-turn completion with no memory or state
* C. A pure retrieval workflow with no reasoning component
* D. A data-labeling pipeline for offline fine-tuning

Answer: 🟩 A. A feedback loop using tool observations to refine behavior before final action

* Why it is correct: A feedback loop using tool observations to refine behavior before final action is correct because it best matches the planning, orchestration, tool-use, or control concept that defines the agent behavior in the scenario.
* Why others are incorrect: The other options describe neighboring AI patterns, but they do not capture the agentic control, orchestration, or action model the question is testing.

## Question 128 (Domain: Generative AI Models)
An engineer is analyzing a batch of model outputs and notices that some source documents were retrieved from outdated policy folders. What is the most useful next step?

* A. Trace retrieval metadata and inspect whether stale documents were indexed or ranked too highly
* B. Lower the temperature so the retriever stops returning old documents
* C. Replace the vector database with a larger language model immediately
* D. Hide source citations from reviewers so they focus only on the final answer quality

Answer: 🟩 A. Trace retrieval metadata and inspect whether stale documents were indexed or ranked too highly

* Why it is correct: Trace retrieval metadata and inspect whether stale documents were indexed or ranked too highly is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 129 (Domain: Ethics and Security)
What is the strongest reason to separate training, validation, and test datasets when evaluating an AI workflow?

* A. It reduces the risk of overstating quality by measuring on data already used for tuning decisions
* B. It guarantees that the deployed system will never drift after release
* C. It allows production prompts to bypass schema validation safely
* D. It removes the need to define an evaluation rubric in advance

Answer: 🟩 A. It reduces the risk of overstating quality by measuring on data already used for tuning decisions

* Why it is correct: It reduces the risk of overstating quality by measuring on data already used for tuning decisions is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.

## Question 130 (Domain: Ethics and Security)
A reviewer wants to understand why one summarization workflow performs worse on security incidents than on routine service requests. Which analysis is most appropriate?

* A. Segment results by scenario type and compare error patterns across those subsets
* B. Average all results together so outlier categories do not distract from the overall score
* C. Increase response length limits for every prompt and assume the gap will disappear
* D. Replace the benchmark with a single anecdotal example from a successful run

Answer: 🟩 A. Segment results by scenario type and compare error patterns across those subsets

* Why it is correct: Segment results by scenario type and compare error patterns across those subsets is correct because it best matches the data-analysis or data-preparation activity that best matches the situation in the prompt.
* Why others are incorrect: The other options refer to different lifecycle stages, unrelated controls, or outcomes that do not solve the stated data-analysis problem.
